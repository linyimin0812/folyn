{
  "name": "@quill/plugin-graphviz",
  "version": "0.1.0",
  "description": "Graphviz (DOT) renderer — trusted-tier Quill plugin (file-type + :::graphviz container)",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@viz-js/viz": "^3.28.0"
  },
  "devDependencies": {
    "@quill/plugin-sdk": "workspace:*",
    "@types/react": "^18.3.12",
    "react": "^18.3.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.8"
  }
}
