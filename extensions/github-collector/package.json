{
  "name": "@folyn/extension-github-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub activity collector — polls the user's GitHub event stream into the activity timeline",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "folyn-extension-sdk": "workspace:*",
    "@types/node": "^22.10.2",
    "esbuild": "^0.23.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
