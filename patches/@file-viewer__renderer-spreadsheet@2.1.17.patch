diff --git a/dist/spreadsheet/state.js b/dist/spreadsheet/state.js
index 819db3cd6864cb62ead5c777946ea1b93e2b5da4..456e28313a6df8ab78de5fe2079c43f11d834175 100644
--- a/dist/spreadsheet/state.js
+++ b/dist/spreadsheet/state.js
@@ -5,7 +5,7 @@ export const INDEX_COLUMN_KEY = '__index';
 export const ROW_KEY_FIELD = '__k';
 export const ROW_STATE_FIELD = '__s';
 export const DEFAULT_SHEET_DEFAULTS = {
-    rowHeight: 20,
+    rowHeight: 22,
     colWidth: 64
 };
 export const RowState = {
diff --git a/dist/spreadsheet/view.js b/dist/spreadsheet/view.js
index 3afd8fc6ab972a10857168502feb23ac9ade77e5..b0b5ba25e7801890b83d8b7183de2b72d700970f 100644
--- a/dist/spreadsheet/view.js
+++ b/dist/spreadsheet/view.js
@@ -1,13 +1,17 @@
 import tinycolor from 'tinycolor2';
 import { displayCellKey, getDataKey, getRowState, INDEX_COLUMN_KEY, ROW_KEY_FIELD, RowState } from './state.js';
 export const INDEX_COLUMN_WIDTH = 68;
+export const computeIndexColumnWidth = (totalRows = 0) => {
+    const digits = Math.max(1, String(totalRows || 1).length);
+    return Math.min(80, Math.max(28, 16 + digits * 9));
+};
 const TABLE_FONT_FAMILY = 'Aptos, Calibri, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif';
-const TABLE_FONT_SIZE = 11;
+const TABLE_FONT_SIZE = 12;
 const MIN_RENDERABLE_ROW_HEIGHT = 8;
 const HIDDEN_ROW_HEIGHT = 0.1;
 const CELL_PADDING = 2;
 const CELL_LINE_HEIGHT = 1.2;
-export const HEADER_HEIGHT = 34;
+export const HEADER_HEIGHT = 22;
 export const RESIZABLE_COLUMN_MIN_WIDTH = 40;
 export const RESIZABLE_ROW_MIN_HEIGHT = 18;
 const EXCEL_HEADER_BG = '#f3f3f3';
@@ -232,17 +236,18 @@ export const detectIndexOffset = (ws) => {
     return 1;
 };
 export const buildColumns = (ws) => {
-    var _a;
+    var _a, _b;
     const columnsMeta = getSheetColumnsMeta(ws);
     const colWidths = getSheetColWidths(ws);
+    const indexWidth = computeIndexColumnWidth(((_b = (_a = ws.meta) === null || _a === void 0 ? void 0 : _a.totalRows) !== null && _b !== void 0 ? _b : 0) || 0);
     const columns = [{
             key: INDEX_COLUMN_KEY,
             title: '',
             type: 'index',
             fixed: 'left',
-            width: INDEX_COLUMN_WIDTH,
-            minWidth: INDEX_COLUMN_WIDTH,
-            maxWidth: INDEX_COLUMN_WIDTH,
+            width: indexWidth,
+            minWidth: indexWidth,
+            maxWidth: indexWidth,
             widthFillDisable: true,
             headerAlign: 'center',
             align: 'center',
@@ -264,7 +269,7 @@ export const buildColumns = (ws) => {
             title: (column === null || column === void 0 ? void 0 : column.title) || `${index + 1}`,
             width: hidden ? 0 : Math.max(Math.ceil(width), 1),
             hide: hidden,
-            widthFillDisable: true,
+            widthFillDisable: false,
             renderType: 'both',
             headerAlign: 'center',
             headerVerticalAlign: 'middle',
@@ -847,6 +852,7 @@ export const createTableConfig = ({ hostHeight, resizableColumns = false, resiza
         PLACEHOLDER_COLOR: '#8a94a3',
         SCROLLER_COLOR: '#c1c7d0',
         SCROLLER_FOCUS_COLOR: '#9aa0a6',
+        SCROLLER_TRACK_SIZE: 0,
         SELECT_ROW_COL_BG_COLOR: EXCEL_GREEN_SOFT,
         SELECT_AREA_COLOR: 'rgba(33, 163, 102, 0.14)',
         SELECT_BORDER_COLOR: EXCEL_GREEN,
diff --git a/dist/spreadsheet.js b/dist/spreadsheet.js
index 117229afdf2c1a8f477c7c69595d2f3a240d6904..234ff65aac339bcbf4ad2e92ae1e271ca5b9c60b 100644
--- a/dist/spreadsheet.js
+++ b/dist/spreadsheet.js
@@ -405,6 +405,15 @@ const copyTextWithTextareaFallback = (documentRef, text) => {
 };
 const writeSpreadsheetClipboard = async (documentRef, text) => {
     var _a;
+    if (typeof globalThis !== 'undefined' && globalThis.__TAURI_INTERNALS__) {
+        try {
+            const mod = await import('@tauri-apps/plugin-clipboard-manager');
+            await mod.writeText(text);
+            return true;
+        } catch (e) {
+            console.error('Tauri clipboard writeText failed:', e);
+        }
+    }
     const targetWindow = documentRef.defaultView;
     const clipboard = (_a = targetWindow === null || targetWindow === void 0 ? void 0 : targetWindow.navigator) === null || _a === void 0 ? void 0 : _a.clipboard;
     const useAsyncClipboard = !!(targetWindow === null || targetWindow === void 0 ? void 0 : targetWindow.isSecureContext) && typeof (clipboard === null || clipboard === void 0 ? void 0 : clipboard.writeText) === 'function';
@@ -747,6 +756,25 @@ const renderFileViewerSpreadsheet = async (buffer, target, _type, context) => {
             console.error('Spreadsheet copy failed:', error);
         });
     };
+    // macOS Edit menu's Cmd+C accelerator preempts the webview keydown, so
+    // e-virt-table's keydown-based copy never fires. The menu dispatches a
+    // native `copy` event instead — intercept it here and route to the same
+    // copy pipeline. Textareas/text selections fall through to native copy.
+    const handleDocumentCopy = (event) => {
+        if (disposed || !table) return;
+        const active = documentRef.activeElement;
+        const target = event.target;
+        const insideTable = (target && tableHost.contains(target)) || (active && tableHost.contains(active));
+        if (!insideTable) return;
+        const ctx = table.ctx;
+        if (!ctx || ctx.textSelectionStr || ctx.domSelectionStr) return;
+        if (typeof ctx.getSelectedData !== 'function') return;
+        const { value, xArr, yArr } = ctx.getSelectedData();
+        if (!xArr || !yArr || (xArr.length === 0 && yArr.length === 0)) return;
+        event.preventDefault();
+        copySpreadsheetSelection({ focusCell: ctx.focusCell, data: value, xArr, yArr });
+    };
+    documentRef.addEventListener('copy', handleDocumentCopy);
     const buildTableView = () => ({
         config: createTableConfig({
             hostHeight: getHostHeight(),
@@ -1340,6 +1368,7 @@ const renderFileViewerSpreadsheet = async (buffer, target, _type, context) => {
         unmount() {
             var _a;
             disposed = true;
+            documentRef.removeEventListener('copy', handleDocumentCopy);
             if (resizeFrame) {
                 cancelAnimationFrame(resizeFrame);
             }
