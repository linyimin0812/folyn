{"file": "docs/adr/0002-custom-provider-adapter-family-direct.md", "reason": "Documents the adapter_family dispatch design this task extends. Confirms resolve_adapter_family returns params.adapter_family.unwrap_or(provider.as_str()) — explains why the 8 new native arms + shrinking the _ arm work without touching ChatParams. Also warns against unifying catalog's defaultChatEndpoint with the Rust match (a likely-looking but wrong refactor to avoid during implementation)."}
{"file": ".trellis/spec/desktop/frontend/index.md", "reason": "ChatParams fields cross the TS/Rust boundary as camelCase via serde. Match-arm additions must preserve the existing adapterFamily contract (TS sends adapterFamily, Rust falls back to provider.as_str() when absent). No direct frontend edit."}
