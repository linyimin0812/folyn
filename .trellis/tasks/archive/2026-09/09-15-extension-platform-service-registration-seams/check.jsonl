{"_example": "Fill with {\"file\": \"<path>\", \"reason\": \"<why>\"}. Put spec/research files only — no code paths. Run `python3 .trellis/scripts/get_context.py --mode packages` to list available specs. Delete this line once real entries are added."}
{"file": "extension-system.SPEC.md", "reason": "Verify refactor preserves the 5 invariants: manifest validation first; sandbox never gets raw host handle; trusted never activates without TOFU+integrity; all contributions owned (OwnedRegistry removeByOwner still evicts on deactivate); install never trusts zip contents. Registry fold must not break transactional rollback."}
{"file": ".trellis/spec/desktop/frontend/quality-guidelines.md", "reason": "Review checklist + forbidden patterns. Verify no one-impl interfaces introduced, no default exports except App, behavior parity with existing extensions."}
{"file": ".trellis/spec/desktop/frontend/trusted-plugin-rendering.md", "reason": "Verify trusted rendering contract intact: host window.React singletons, blob-URL import, normalizeModule pulls the same export keys as before (no missing, no extra)."}
{"file": ".trellis/spec/desktop/frontend/index.md", "reason": "Quality Check section for the desktop frontend — the acceptance bar for changed desktop files (loaders, capabilities, App.tsx boot)."}
